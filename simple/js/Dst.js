// daylight saving time
function set_dst() {

    dst = 0;    //the time zone is changes be default due to the year period
    return;

    //var date = new Date();

    //var d = date.getDate() + 1;
    //var m = date.getMonth() + 1;


    //if ((m == 10 && d == 30) || m > 10 || m < 3 || (m == 3 && d < 24))
    //    dst = 0;
    //else
    //    dst = 1;

    //document.getElementById("stop").value = dst;
}